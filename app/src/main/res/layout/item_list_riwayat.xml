<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModelRiwayat"
            type="com.azizapp.test.ui.riwayat.RiwayatViewModel" />

        <variable
            name="data"
            type="com.azizapp.test.model.Pengaduan" />

        <variable
            name="position"
            type="Integer" />

        <variable
            name="RiwayatFragment"
            type="com.azizapp.test.ui.riwayat.RiwayatFragment" />


    </data>

    <androidx.cardview.widget.CardView
        android:id="@+id/rl_klik"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:backgroundTint="@color/white"
        android:elevation="30dp"
        android:onClick="@{()->viewModelRiwayat.itemOnClick(position)}"
        app:cardCornerRadius="8dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/image_pin"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_centerVertical="true"
                android:layout_marginHorizontal="6dp"
                android:rotation="30"
                app:srcCompat="@drawable/ic_pin" />

            <TextView
                android:id="@+id/tvLokasi"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="6dp"
                android:layout_marginTop="16dp"
                android:layout_toEndOf="@id/image_pin"
                android:ellipsize="end"
                android:fontFamily="@font/poppins_regular"
                android:maxLines="2"
                android:text="@={data.namaJalan}"
                android:textColor="@color/black"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/tv_jenis_pengaduan"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/tvLokasi"
                android:layout_marginHorizontal="6dp"
                android:layout_marginTop="6dp"
                android:layout_toEndOf="@id/image_pin"
                android:fontFamily="@font/poppins_regular"
                android:text="@{@string/tipe_pengaduan+data.tipePengaduan}"
                android:textColor="@color/black"
                android:textSize="12sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/tv_jenis_pengaduan"
                android:layout_marginBottom="10dp"
                android:layout_marginStart="10dp"
                android:layout_toEndOf="@id/image_pin"
                android:fontFamily="@font/poppins_bold"
                android:paddingVertical="6dp"
                android:setTextColorFromStatus="@{data.statusPengaduan}"
                android:text="@={data.statusPengaduan}"
                android:textColor="@color/orange"
                android:textSize="14sp" />

        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>